-functions to test:
--In group_analysis
	   -loading data
	   -list of mice to include
	   -calculating dF/F
	   -timing (this should be correct, everything
	   	   should be based off self.time_stamps)
           -group_bout_heatmaps
	   -group_regression_plot
	   -group_bout_ci (? I don't think we are using this)
	   -group_plot_time_series
	   -plot_representative_time_series
		-clean up representative_time_series_specs.txt
	   -get_novel_social_pairs
	   -score_of_chunks
	   -compare_start_and_end_of_epoch
	   -statisticalTestOfComparison (also make sure we always use  wilcoxon?)
	   -compare_epochs
	   -plot_decay
	   -compare_decay (and plot_decay, and fit_exponential)
	   -event_length_histogram
	   --option parser defaults, and command line arguments
	   -merge fit_exponential

--In FiberAnalyze
	   -initializiations in FiberAnalyze
	   -load (make sure normalization is correct)
	   -crop_data
	   -load_trigger_data
	   -normalize_fluorescence_data
	   -load_event_data
	   $-set_resolution and get_plot_ylim are fine
	   -plot_basic_tseries
	   
	   -check the h5 file

	   -plot_next_event_vs_intensity (this is a key one?)
	   	-should we combine the score function here into 
			-the other score function in group_analysis?
	   -do we use smooth? default to 0. We use wavelets, no?
	   -get_time_chunks_around_events (this is KEY)
	   -plot_perievent_hist
	   -plot_peritrigger_edge
	   -get_event_times
	   -get_sucrose_event_times
	   -debleach (did we want to make this more robust?)
	   -get_areas_under_curve (do we want to combine this with
	   			  our other score function?)
	   -get_peak
	   -fit_exponential 
	   -plot_peaks_vs_time (I don't think we are using this right now)

	   
	   --Preprocessing

--Make better wrapper for running analysis
